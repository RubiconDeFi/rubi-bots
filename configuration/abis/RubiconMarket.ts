import { Interface } from '@ethersproject/abi';
import RUBICON_MARKET from './RubiconMarket.json';

export const RUBICON_MARKET_INTERFACE = new Interface(RUBICON_MARKET);

export default RUBICON_MARKET_INTERFACE;