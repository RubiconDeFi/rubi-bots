import { Interface } from '@ethersproject/abi';
import RUBICON_ROUTER from './RubiconRouter.json';

export const RUBICON_ROUTER_INTERFACE = new Interface(RUBICON_ROUTER);

export default RUBICON_ROUTER_INTERFACE;